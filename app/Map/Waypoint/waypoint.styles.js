var React = require('react-native');
var { StyleSheet } = React;

var styles = StyleSheet.create({

  scrollView: {
    height: 200,
  },

  description: {
    color: "#fff",
    fontSize: 18,
    padding: 10,
  }

});

module.exports = styles;
/* 
*  So your code doesn't break, add "secrets.js" to your root directory, .gitignore it,
*  and copy the contents of this file over to it.
*  Add any necessary keys as properties.
*/

var keys = {};

keys.mapQuest = 'YOUR-API-KEY-HERE';

module.exports = keys;